<Window x:Class="TripTailorSimple.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TripTailorSimple.WPF.ViewModels"
        mc:Ignorable="d"
        Title="TripTailor Simple"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Recherche de destination"
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Budget max" Margin="0,0,0,5"/>
                <TextBox Width="120" Text="{Binding Budget, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Région" Margin="0,0,0,5"/>
                <ComboBox Width="180"
                          ItemsSource="{Binding Regions}"
                          SelectedItem="{Binding SelectedRegion}"/>
            </StackPanel>

            <Button Content="Rechercher"
                    Width="120"
                    Height="35"
                    VerticalAlignment="Bottom"
                    Command="{Binding SearchCommand}"/>

            <Button Content="Ouvrir le chatbot"
                    Width="160"
                    Height="35"
                    Margin="12,0,0,0"
                    VerticalAlignment="Bottom"
                    Click="OpenChatWindow_Click"/>
        </StackPanel>

        <ListView Grid.Row="2" ItemsSource="{Binding Results}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="12"
                            Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="{Binding CityCountry}"
                                       FontSize="20"
                                       FontWeight="Bold"/>
                            <TextBlock Text="{Binding Region}"
                                       FontStyle="Italic"
                                       Margin="0,4,0,4"/>
                            <TextBlock Text="{Binding Description}"
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Prix : " FontWeight="Bold"/>
                                <TextBlock Text="{Binding PriceDisplay}" Margin="0,0,20,0"/>
                                <TextBlock Text="Température : " FontWeight="Bold"/>
                                <TextBlock Text="{Binding TemperatureDisplay}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>