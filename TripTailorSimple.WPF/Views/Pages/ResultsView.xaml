<UserControl x:Class="TripTailorSimple.WPF.Views.Pages.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:TripTailorSimple.WPF.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:EmptyListToVisibilityConverter x:Key="EmptyListToVisibilityConverter" />
    </UserControl.Resources>

    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="← Modifier la recherche" Command="{Binding BackToSearchCommand}" />
        </StackPanel>

        <TextBlock Text="Aucun résultat trouvé pour ces critères."
                   Visibility="{Binding Results, Converter={StaticResource EmptyListToVisibilityConverter}}"
                   FontSize="18"
                   Foreground="DarkRed" />

        <ListView ItemsSource="{Binding Results}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{Binding CityCountry}" FontSize="22" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" Margin="0,4,0,6"/>
                                <TextBlock Text="{Binding Temperature}"/>
                                <TextBlock Text="{Binding Price}" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding Breakdown}" Foreground="DimGray" Margin="0,4,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                                <TextBlock Text="{Binding TravelBadge}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="DarkSlateBlue" Margin="0,0,0,8"/>
                                <Button Content="Voir le séjour"
                                        Command="{Binding DataContext.OpenDetailCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</UserControl>
