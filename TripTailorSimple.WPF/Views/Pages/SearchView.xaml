<UserControl x:Class="TripTailorSimple.WPF.Views.Pages.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:TripTailorSimple.WPF.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:StringEqualsConverter x:Key="StringEqualsConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Planifie ton voyage idéal"
                       FontSize="30"
                       FontWeight="Bold"
                       Margin="0,0,0,16"/>

            <WrapPanel Margin="0,0,0,12">
                <StackPanel Margin="0,0,14,0">
                    <TextBlock Text="Budget max (€)"/>
                    <TextBox Width="120" Text="{Binding Budget, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Margin="0,0,14,0">
                    <TextBlock Text="Durée (jours)"/>
                    <TextBox Width="120" Text="{Binding DurationDays, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Margin="0,0,14,0">
                    <TextBlock Text="Saison"/>
                    <ComboBox Width="140" ItemsSource="{Binding Seasons}" SelectedItem="{Binding SelectedSeason}"/>
                </StackPanel>

                <StackPanel Margin="0,0,14,0">
                    <TextBlock Text="Climat"/>
                    <ComboBox Width="140" ItemsSource="{Binding Climates}" SelectedItem="{Binding SelectedClimate}"/>
                </StackPanel>

                <StackPanel Margin="0,0,14,0">
                    <TextBlock Text="Style de voyage"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Économie" Margin="0,0,8,0" Padding="10,4" Command="{Binding SelectStyleCommand}" CommandParameter="Économie"/>
                        <Button Content="Confort" Margin="0,0,8,0" Padding="10,4" Command="{Binding SelectStyleCommand}" CommandParameter="Confort"/>
                        <Button Content="Luxe" Padding="10,4" Command="{Binding SelectStyleCommand}" CommandParameter="Luxe"/>
                    </StackPanel>
                </StackPanel>
            </WrapPanel>

            <TextBlock Text="Régions" FontWeight="SemiBold"/>
            <ItemsControl ItemsSource="{Binding Regions}" Margin="0,6,0,10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Margin="0,0,12,8"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Type de voyage" FontWeight="SemiBold"/>
            <ComboBox Margin="0,6,0,10" Width="180" ItemsSource="{Binding TripTypes}" SelectedItem="{Binding SelectedTripType}" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                <CheckBox Content="Inclure vol" IsChecked="{Binding IncludeFlight}" Margin="0,0,14,0"/>
                <CheckBox Content="Inclure hôtel" IsChecked="{Binding IncludeHotel}" Margin="0,0,14,0"/>
                <CheckBox Content="Inclure activités" IsChecked="{Binding IncludeActivities}" />
            </StackPanel>

            <Button Content="Rechercher des séjours"
                    Width="220"
                    Height="40"
                    FontWeight="SemiBold"
                    Command="{Binding DataContext.SearchCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
